# MAISON LUMIÈRE — media status

Present: `bg.mp4` (plain copy — re-encode with `scripts/encode-scroll-video.sh`
on a machine with ffmpeg for frame-accurate scrubbing), `images/*.png`
(interior-hall, detail-table, detail-bar, terrace-night, dish-2 for the menu
preview).

Still required: `cutouts/dish-1-cutout.png` … `cutouts/dish-6-cutout.png` —
the transparent dish cutouts (already generated, see assets/MANIFEST.md for
download links). Until they are added, the dish cards show elegant
placeholders instead of the pop-out effect.
